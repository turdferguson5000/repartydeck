name: CI (Holo / Steam Deck)
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Build Holo image
        run: |
          docker build \
            --build-arg UID=$(id -u) \
            --build-arg GID=$(id -g) \
            -t partydeck-holo-build \
            -f dist/Dockerfile.holo .

      - name: Build release skeleton
        run: docker run --rm -v "$PWD:/workspace" partydeck-holo-build

      - name: Create tarball
        run: |
          tar -czf "partydeck-UNOFFICIAL-HOLO-TEST-steamdeck-x86_64.tar.gz" \
              -C dist/build_generated/holo release/

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: built-UNOFFICIAL-binaries-holo
          path: |
            partydeck-UNOFFICIAL-HOLO-TEST-steamdeck-x86_64.tar.gz
